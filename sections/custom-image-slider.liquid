{% comment %} Section: Custom Image Slider - 3 visible slides, 400x400, padding, auto scroll, bottom spacing {% endcomment %}
<div class="custom-slider-container">
  <div class="custom-slider-track">
    {% for block in section.blocks %}
      {% if block.settings.image %}
        <div class="custom-slide">
          <img 
            src="{{ block.settings.image | image_url: width: 400, height: 400, crop: 'center' }}" 
            width="400" 
            height="400" 
            alt="{{ block.settings.image.alt | escape }}">
        </div>
      {% endif %}
    {% endfor %}
  </div>
</div>

<style>
  .custom-slider-container {
    width: calc((400px + 20px) * 3); /* 3 slides + padding */
    overflow: hidden;
    margin: 0 auto;
    position: relative;
    padding-bottom: 40px; /* Adds space below the slider */
  }

  .custom-slider-track {
    display: flex;
    gap: 20px;
    width: max-content;
    animation: scrollSlides 12s linear infinite;
  }

  .custom-slide {
    flex: 0 0 400px;
    height: 400px;
    display: flex;
    justify-content: center;
    align-items: center;
  }

  @keyframes scrollSlides {
    0%   { transform: translateX(0); }
    25%  { transform: translateX(calc(-1 * (400px + 20px))); }
    50%  { tr
